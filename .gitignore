# Build outputs & Gradle caches (regenerated by any build)
DecoderSpike/build/
DecoderSpike/app/build/
DecoderSpike/.gradle/
DecoderSpike/build.log

# Machine-specific paths — never commit (SDK location differs per machine)
DecoderSpike/local.properties

# IDE state
.idea/
*.iml

# Local Claude Code session settings
.claude/

# MultiCam app build outputs
MultiCam/build/
MultiCam/app/build/
MultiCam/.gradle/
MultiCam/build.log
MultiCam/local.properties
